# RTK

A hook transparently rewrites Bash commands through `rtk` (token-saving proxy) — run commands normally. `rtk proxy <cmd>` runs one unfiltered; `rtk gain` shows savings. If `rtk gain` fails, the wrong `rtk` (Rust Type Kit) may be installed.
