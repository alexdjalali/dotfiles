# Linear

Only for repos tracked in Linear (a repo's own `.claude/rules/linear.md` wins). Tickets come from `~/.claude/templates/linear/` (see its `README.md`): Background → Requirements → Acceptance Criteria, skimmable in ten seconds; engineering discipline stays in the repo, not the ticket. Mirror and cross-link the `docs/spec/` artifact. **⛔ Never fabricate a ticket id** — read it from the story/plan/PR/branch or use `(unticketed — create in <TEAM>)`. Mutate Linear only via an authenticated Linear MCP; otherwise prepare the text and never claim it changed. Ids go in branch/PR/commit — never in code comments.
